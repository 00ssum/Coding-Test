#https://www.acmicpc.net/problem/2606

N= int(input())
M= int(input())
dica= [set() for _ in range(N+1)]#n+1개 set 준비

for _ in range(M): #edge 수만큼 반복문
    p, q = map(int, input().split())
    dica[p].add(q)
    dica[q].add(p)

#dfs
visited = []
stack = [1]
cont=-1
while stack:
    v = stack.pop()
    if v not in visited: # 가본적 없는 노드
        visited.append(v) #visited에 추가
        cont+=1
        stack.extend(sorted(dica[v]-set(visited), reverse=True))
        #dict[v]에서 visited 에 있는건 빼고 (방문한 노드 빼고) 방문하지 않은 노드들을 내림차순 정렬(작은수가 가장 먼저 나갈수 있게)하여 stack에 붙임
print(cont)
